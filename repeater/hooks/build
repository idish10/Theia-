#!/bin/bash

docker build -t $IMAGE_NAME -f $DOCKERFILE_PATH --build-arg DOCKERHUB_USER=$DOCKERHUB_USER --build-arg DOCKERHUB_PASSWORD=$DOCKERHUB_PASSWORD --build-arg REPEAT_HOURS=$REPEAT_HOURS --build-arg REPEAT_MINUTES=$REPEAT_MINUTES  .
